{
  "name": "wordpress-mcp-connector",
  "version": "1.0.0",
  "private": true,
  "description": "Local WordPress MCP connector with encrypted credential storage and a web management UI.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check bin/run-wordpress-mcp.js && node --check lib/backup.js"
  },
  "engines": {
    "node": ">=20"
  }
}
